specVersion: "v1"
serviceId: "copy-of-oo-of-al-mohaseb-al-zaki-smart-accountant"
location: "us-east4"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "copy-of-oo-of-al-mohaseb-al-zaki-smart-accountant-fdc"
        # schemaValidation: "STRICT"     # STRICT mode makes Postgres schema match Data Connect exactly.
        # schemaValidation: "COMPATIBLE" # COMPATIBLE mode makes Postgres schema compatible with Data Connect.
connectorDirs: ["./example"]
